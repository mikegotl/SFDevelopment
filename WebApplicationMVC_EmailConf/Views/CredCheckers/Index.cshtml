@model IEnumerable<WebApplicationMVC_EmailConf.Models.CredChecker>

@{
    ViewBag.Title = "Index";
}

<h2>Credibility Checker</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CCID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CCID)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CCID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CCID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CCID })
        </td>
    </tr>
}

</table>
